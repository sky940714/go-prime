import React, { useState, useMemo, useEffect } from 'react';
import {
  View, Text, StyleSheet, TouchableOpacity, 
  TextInput, Dimensions, Platform, ScrollView
} from 'react-native';
import { router } from 'expo-router';
import {
  X, Sparkles, Palette, Zap, Clock,
  User, DollarSign, ChevronRight, ChevronLeft, MapPin,
  Search, CheckCircle2
} from 'lucide-react-native';
import * as Haptics from 'expo-haptics';
import Animated, { 
  FadeInRight, 
  FadeOutLeft, 
  Layout,
  useSharedValue,
  useAnimatedStyle,
  withSpring
} from 'react-native-reanimated';
import { Colors } from '@/constants/Colors';

const { width, height } = Dimensions.get('window');

// ─── 資料庫 ───────────────────────────────────────────────
const TAIPEI_METRO_STATIONS = [
  '台北車站', '中山', '西門', '東區/忠孝復興', '市政府', '信義安和',
  '南京復興', '公館', '板橋', '淡水', '松山', '內湖', '行天宮', '古亭'
];

const SERVICE_OPTIONS = {
  '美甲': ['單色/貓眼', '法式/漸層', '設計款式', '卸甲重做', '延甲'],
  '醫美': ['臉部微調', '皮膚管理', '雷射療程', '身體塑形'],
  '除毛': ['腋下', '手臂', '全腿', '私密處'],
  '刺青': ['美式文字', '日系大圖', '微刺青', '改圖/蓋圖'],
};

const BUDGET_RANGES = [
  '500 內', '500 - 1,000', '1,000 - 2,000', '2,000 - 3,500',
  '3,500 - 5,500', '5,500 - 8,000', '8,000 - 12,000', '12,000 - 20,000'
];

export default function ActionScreen() {
  const [step, setStep] = useState(1);
  const totalSteps = 6;

  // 表單狀態
  const [selectedLocation, setSelectedLocation] = useState('');
  const [locationSearch, setLocationSearch] = useState('');
  const [selectedType, setSelectedType] = useState('美甲');
  const [selectedService, setSelectedService] = useState('');
  const [userGender, setUserGender] = useState('女生');
  const [designerPref, setDesignerPref] = useState('不限');
  const [timing, setTiming] = useState('今天');
  const [selectedBudget, setSelectedBudget] = useState('');

  // 搜尋過濾
  const filteredStations = useMemo(() => {
    if (!locationSearch || selectedLocation === locationSearch) return [];
    return TAIPEI_METRO_STATIONS.filter(s => s.includes(locationSearch));
  }, [locationSearch, selectedLocation]);

  // 進度條動畫
  const progress = useSharedValue(1 / totalSteps);
  useEffect(() => {
    progress.value = withSpring(step / totalSteps);
  }, [step]);

  const progressStyle = useAnimatedStyle(() => ({
    width: `${progress.value * 100}%`,
  }));

  const handleNext = () => {
    if (step < totalSteps) {
      Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
      setStep(step + 1);
    } else {
      finalizeMatch();
    }
  };

  const handleBack = () => {
    if (step > 1) {
      Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);
      setStep(step - 1);
    } else {
      router.back();
    }
  };

  const finalizeMatch = () => {
    Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);
    router.push({
        pathname: '/match',
        params: { location: selectedLocation, service: selectedService }
    });
  };

  // ─── 步驟渲染組件 ──────────────────────────────────────────

  const Step1Location = () => (
    <Animated.View entering={FadeInRight} exiting={FadeOutLeft} style={styles.stepContainer}>
      <MapPin size={42} color={Colors.stone[900]} style={styles.stepIcon} />
      <Text style={styles.stepTitle}>服務地點在哪裡？</Text>
      <Text style={styles.stepDesc}>選擇您方便抵達的捷運站或地區</Text>
      
      <View style={styles.searchBox}>
        <Search size={20} color={Colors.stone[400]} style={{ marginRight: 12 }} />
        <TextInput
          style={styles.locationInput}
          placeholder="搜尋台北捷運站..."
          value={locationSearch}
          onChangeText={setLocationSearch}
          placeholderTextColor={Colors.stone[300]}
        />
      </View>

      <ScrollView style={styles.stationList} showsVerticalScrollIndicator={false}>
        {(locationSearch ? filteredStations : TAIPEI_METRO_STATIONS).map(station => (
          <TouchableOpacity 
            key={station} 
            style={[styles.stationItem, selectedLocation === station && styles.stationItemActive]}
            onPress={() => {
              setSelectedLocation(station);
              setLocationSearch(station);
              Haptics.selectionAsync();
              setTimeout(() => setStep(2), 300); // 選完自動跳下一步
            }}
          >
            <Text style={[styles.stationText, selectedLocation === station && styles.stationTextActive]}>{station}</Text>
            {selectedLocation === station && <CheckCircle2 size={18} color={Colors.stone[900]} />}
          </TouchableOpacity>
        ))}
      </ScrollView>
    </Animated.View>
  );

  const Step2Service = () => (
    <Animated.View entering={FadeInRight} exiting={FadeOutLeft} style={styles.stepContainer}>
      <Palette size={42} color={Colors.stone[900]} style={styles.stepIcon} />
      <Text style={styles.stepTitle}>想要什麼服務？</Text>
      
      <View style={styles.categoryRow}>
        {Object.keys(SERVICE_OPTIONS).map((name) => (
          <TouchableOpacity
            key={name}
            onPress={() => {
                Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);
                setSelectedType(name);
                setSelectedService('');
            }}
            style={[styles.typeBtn, selectedType === name && styles.typeBtnActive]}
          >
            <Text style={[styles.typeBtnText, selectedType === name && styles.typeBtnTextActive]}>{name}</Text>
          </TouchableOpacity>
        ))}
      </View>

      <View style={styles.chipGrid}>
        {SERVICE_OPTIONS[selectedType as keyof typeof SERVICE_OPTIONS].map((item) => (
          <TouchableOpacity
            key={item}
            onPress={() => {
                setSelectedService(item);
                Haptics.selectionAsync();
            }}
            style={[styles.chip, selectedService === item && styles.chipActive]}
          >
            <Text style={[styles.chipText, selectedService === item && styles.chipTextActive]}>{item}</Text>
          </TouchableOpacity>
        ))}
      </View>
    </Animated.View>
  );

  const Step3Preference = () => (
    <Animated.View entering={FadeInRight} exiting={FadeOutLeft} style={styles.stepContainer}>
      <User size={42} color={Colors.stone[900]} style={styles.stepIcon} />
      <Text style={styles.stepTitle}>關於您的偏好</Text>
      
      <Text style={styles.subLabel}>您的性別</Text>
      <View style={styles.toggleRow}>
        {['女生', '男生'].map((g) => (
          <TouchableOpacity key={g} style={[styles.toggleBtn, userGender === g && styles.toggleBtnActive]} onPress={() => setUserGender(g)}>
            <Text style={[styles.toggleText, userGender === g && styles.toggleTextActive]}>{g}</Text>
          </TouchableOpacity>
        ))}
      </View>

      <Text style={[styles.subLabel, { marginTop: 32 }]}>偏好施作人員</Text>
      <View style={styles.toggleRow}>
        {['不限', '女性', '男性'].map((p) => (
          <TouchableOpacity key={p} style={[styles.toggleBtn, designerPref === p && styles.toggleBtnActive]} onPress={() => setDesignerPref(p)}>
            <Text style={[styles.toggleText, designerPref === p && styles.toggleTextActive]}>{p}</Text>
          </TouchableOpacity>
        ))}
      </View>
    </Animated.View>
  );

  const Step4Timing = () => (
    <Animated.View entering={FadeInRight} exiting={FadeOutLeft} style={styles.stepContainer}>
      <Clock size={42} color={Colors.stone[900]} style={styles.stepIcon} />
      <Text style={styles.stepTitle}>什麼時候方便？</Text>
      
      <View style={styles.verticalList}>
        {['今天', '明天', '本週末', '不限時間'].map((item) => (
          <TouchableOpacity 
            key={item} 
            style={[styles.verticalItem, timing === item && styles.verticalItemActive]}
            onPress={() => {
                setTiming(item);
                Haptics.selectionAsync();
            }}
          >
            <Text style={[styles.verticalItemText, timing === item && styles.verticalItemTextActive]}>{item}</Text>
            <View style={[styles.radio, timing === item && styles.radioActive]} />
          </TouchableOpacity>
        ))}
      </View>
    </Animated.View>
  );

  const Step5Budget = () => (
    <Animated.View entering={FadeInRight} exiting={FadeOutLeft} style={styles.stepContainer}>
      <DollarSign size={42} color={Colors.stone[900]} style={styles.stepIcon} />
      <Text style={styles.stepTitle}>預算範圍</Text>
      <Text style={styles.stepDesc}>讓我們為您篩選合適價位的店家</Text>

      <View style={styles.budgetGrid}>
        {BUDGET_RANGES.map((b) => (
          <TouchableOpacity 
            key={b} 
            style={[styles.budgetCard, selectedBudget === b && styles.budgetCardActive]}
            onPress={() => {
                setSelectedBudget(b);
                Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);
                setTimeout(() => setStep(6), 300);
            }}
          >
            <Text style={[styles.budgetText, selectedBudget === b && styles.budgetTextActive]}>{b}</Text>
          </TouchableOpacity>
        ))}
      </View>
    </Animated.View>
  );

  const Step6Confirm = () => (
    <Animated.View entering={FadeInRight} exiting={FadeOutLeft} style={styles.stepContainer}>
      <Sparkles size={42} color={Colors.amber400} style={styles.stepIcon} />
      <Text style={styles.stepTitle}>最後確認需求</Text>
      
      <View style={styles.summaryCard}>
        <SummaryRow icon={MapPin} label="地點" value={selectedLocation} />
        <SummaryRow icon={Palette} label="項目" value={`${selectedType} - ${selectedService}`} />
        <SummaryRow icon={User} label="偏好" value={`${userGender} / ${designerPref}人員`} />
        <SummaryRow icon={Clock} label="時間" value={timing} />
        <SummaryRow icon={DollarSign} label="預算" value={selectedBudget} isLast />
      </View>

      <Text style={styles.confirmNotice}>
        點擊確認後，系統將會即時發送給附近符合條件的店家，請留意您的訊息通知。
      </Text>
    </Animated.View>
  );

  return (
    <View style={styles.container}>
      {/* 頂部導航與進度 */}
      <View style={styles.header}>
        <TouchableOpacity onPress={handleBack} style={styles.headerAction}>
          <ChevronLeft size={24} color={Colors.stone[900]} />
        </TouchableOpacity>
        
        <View style={styles.progressTrack}>
          <Animated.View style={[styles.progressFill, progressStyle]} />
        </View>

        <TouchableOpacity onPress={() => router.back()} style={styles.headerAction}>
          <X size={24} color={Colors.stone[900]} />
        </TouchableOpacity>
      </View>

      {/* 主要內容區 */}
      <View style={styles.content}>
        {step === 1 && <Step1Location />}
        {step === 2 && <Step2Service />}
        {step === 3 && <Step3Preference />}
        {step === 4 && <Step4Timing />}
        {step === 5 && <Step5Budget />}
        {step === 6 && <Step6Confirm />}
      </View>

      {/* 底部按鈕 */}
      <View style={styles.footer}>
        <TouchableOpacity 
          style={[styles.nextBtn, (step === 2 && !selectedService) && styles.btnDisabled]} 
          onPress={handleNext}
          disabled={step === 2 && !selectedService}
        >
          <Text style={styles.nextBtnText}>
            {step === totalSteps ? '確認並開始配對' : '繼續下一步'}
          </Text>
          <ChevronRight size={20} color={Colors.white} />
        </TouchableOpacity>
      </View>
    </View>
  );
}

// ─── 子組件 ───────────────────────────────────────────────
const SummaryRow = ({ icon: Icon, label, value, isLast }: any) => (
  <View style={[styles.summaryRow, isLast && { borderBottomWidth: 0 }]}>
    <View style={styles.summaryLeft}>
      <Icon size={16} color={Colors.stone[400]} />
      <Text style={styles.summaryLabel}>{label}</Text>
    </View>
    <Text style={styles.summaryValue}>{value || '未填寫'}</Text>
  </View>
);

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: Colors.white },
  header: { 
    flexDirection: 'row', 
    alignItems: 'center', 
    justifyContent: 'space-between', 
    paddingHorizontal: 20, 
    paddingTop: Platform.OS === 'ios' ? 60 : 40,
    height: 100
  },
  headerAction: { width: 40, height: 40, justifyContent: 'center', alignItems: 'center' },
  progressTrack: { flex: 1, height: 6, backgroundColor: Colors.stone[100], borderRadius: 3, marginHorizontal: 20 },
  progressFill: { height: '100%', backgroundColor: Colors.stone[900], borderRadius: 3 },
  
  content: { flex: 1 },
  stepContainer: { flex: 1, padding: 32 },
  stepIcon: { marginBottom: 20 },
  stepTitle: { fontSize: 28, fontWeight: '900', color: Colors.stone[900], marginBottom: 8 },
  stepDesc: { fontSize: 15, color: Colors.stone[400], marginBottom: 32, fontWeight: '500' },

  // Step 1
  searchBox: { flexDirection: 'row', alignItems: 'center', backgroundColor: Colors.stone[50], borderRadius: 20, paddingHorizontal: 20, height: 60, marginBottom: 24 },
  locationInput: { flex: 1, fontSize: 16, fontWeight: '700', color: Colors.stone[900] },
  stationList: { flex: 1 },
  stationItem: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', paddingVertical: 18, borderBottomWidth: 1, borderBottomColor: Colors.stone[50] },
  stationItemActive: { backgroundColor: Colors.stone[50], paddingHorizontal: 16, borderRadius: 12, borderBottomWidth: 0 },
  stationText: { fontSize: 16, fontWeight: '600', color: Colors.stone[500] },
  stationTextActive: { color: Colors.stone[900], fontWeight: '800' },

  // Step 2
  categoryRow: { flexDirection: 'row', gap: 8, marginBottom: 24 },
  typeBtn: { flex: 1, paddingVertical: 12, borderRadius: 14, backgroundColor: Colors.stone[50], alignItems: 'center' },
  typeBtnActive: { backgroundColor: Colors.stone[900] },
  typeBtnText: { fontSize: 14, fontWeight: '800', color: Colors.stone[400] },
  typeBtnTextActive: { color: Colors.white },
  chipGrid: { flexDirection: 'row', flexWrap: 'wrap', gap: 10 },
  chip: { paddingHorizontal: 20, paddingVertical: 14, borderRadius: 16, backgroundColor: Colors.stone[50], borderWidth: 1.5, borderColor: Colors.stone[50] },
  chipActive: { borderColor: Colors.stone[900], backgroundColor: Colors.white },
  chipText: { fontSize: 15, fontWeight: '700', color: Colors.stone[500] },
  chipTextActive: { color: Colors.stone[900] },

  // Step 3
  subLabel: { fontSize: 13, fontWeight: '800', color: Colors.stone[400], marginBottom: 12, marginLeft: 4 },
  toggleRow: { flexDirection: 'row', backgroundColor: Colors.stone[50], borderRadius: 16, padding: 6 },
  toggleBtn: { flex: 1, paddingVertical: 14, alignItems: 'center', borderRadius: 12 },
  toggleBtnActive: { backgroundColor: Colors.white, elevation: 2, shadowColor: '#000', shadowOpacity: 0.1, shadowRadius: 4 },
  toggleText: { fontSize: 14, fontWeight: '800', color: Colors.stone[300] },
  toggleTextActive: { color: Colors.stone[900] },

  // Step 4
  verticalList: { gap: 12 },
  verticalItem: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', padding: 20, backgroundColor: Colors.stone[50], borderRadius: 20 },
  verticalItemActive: { backgroundColor: Colors.white, borderWidth: 2, borderColor: Colors.stone[900] },
  verticalItemText: { fontSize: 16, fontWeight: '700', color: Colors.stone[400] },
  verticalItemTextActive: { color: Colors.stone[900] },
  radio: { width: 20, height: 20, borderRadius: 10, borderWidth: 2, borderColor: Colors.stone[200] },
  radioActive: { borderColor: Colors.stone[900], borderWidth: 6 },

  // Step 5
  budgetGrid: { flexDirection: 'row', flexWrap: 'wrap', gap: 12 },
  budgetCard: { width: (width - 76) / 2, padding: 20, backgroundColor: Colors.stone[50], borderRadius: 20, alignItems: 'center' },
  budgetCardActive: { backgroundColor: Colors.stone[900] },
  budgetText: { fontSize: 14, fontWeight: '800', color: Colors.stone[500] },
  budgetTextActive: { color: Colors.white },

  // Step 6
  summaryCard: { backgroundColor: Colors.stone[50], borderRadius: 28, padding: 24, marginBottom: 24 },
  summaryRow: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', paddingVertical: 16, borderBottomWidth: 1, borderBottomColor: Colors.stone[100] },
  summaryLeft: { flexDirection: 'row', alignItems: 'center', gap: 8 },
  summaryLabel: { fontSize: 13, fontWeight: '700', color: Colors.stone[400] },
  summaryValue: { fontSize: 15, fontWeight: '800', color: Colors.stone[900] },
  confirmNotice: { fontSize: 13, color: Colors.stone[400], textAlign: 'center', lineHeight: 20, paddingHorizontal: 20 },

  footer: { padding: 24, paddingBottom: Platform.OS === 'ios' ? 44 : 24 },
  nextBtn: { backgroundColor: Colors.stone[900], height: 64, borderRadius: 22, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 8 },
  btnDisabled: { backgroundColor: Colors.stone[200] },
  nextBtnText: { color: Colors.white, fontSize: 17, fontWeight: '900' },
});